(*
    This file can be included by a 3rd party contract in order to
    get the desired entrypoint name
*)
(* 
    Files in this implementation are named `total_token_supply` for better code readability, 
    unlike the entrypoint name itself which is just `total_supply` 
*)
const total_token_supply_entrypoint : string = "%total_supply";